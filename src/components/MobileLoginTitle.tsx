import React from "react";

import { mobileLoginTitle } from "../styles/mobileLoginTitle";

const MobileLoginTitle = () => {
  return (
    <div style={mobileLoginTitle}>
      <h1>Login</h1>
    </div>
  );
};

export default MobileLoginTitle;